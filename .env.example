# Diego CMS — variables de entorno (build-time, nunca se exponen al navegador)
#
# Copia este archivo a `.env` y rellena el valor. `.env` esta git-ignorado.
# En CI (GitHub Actions) el valor vive como secret del repositorio, no en el arbol.

# Token de la integracion privada de Notion (solo lectura). Lo administra Diego.
# Con acceso a exactamente 3 fuentes: SSOT - Portafolio Proyectos,
# Copy Oficial · diegomaury.mx (SSOT) y Metricas oficiales - Portafolio D.
NOTION_TOKEN=
